name: data
description: "data module."
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  injectable: ^2.4.2
  get_it: ^7.7.0
  domain:
    path: ../domain
  common: 
    path: ../common

dev_dependencies:
  injectable_generator: ^2.6.1
  build_runner: ^2.1.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - mock_data/
