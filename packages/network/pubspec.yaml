name: network
description: "network module."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  injectable: ^2.4.2
  get_it: ^7.7.0
  flutter:
    sdk: flutter
  pretty_dio_logger: ^1.3.1
  dio: ^5.4.3+1
  data:
    path: ../data
  domain:
    path: ../domain
  common: 
    path: ../common

dev_dependencies:
  injectable_generator: ^2.6.1
  build_runner: ^2.1.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

